

//We're Back!

/*
NOTES:

private variables for each characteristic
  Constructure takes all of them
  this.

  Accsessors
    public String getName() {return name;}
      shortened version^


seperate class to manipulate the other one
  make an ArrayList and use the Constructre to add characteristics to the AL
  print out stuff in the AL with For Each Loop
    a.getName()/a.getType();


function for Average Number of Legs
  give it an AL

    //For Loop
    float total = 0;

    for(int i = 0; i  < animals.size; i++){

      //total+=animals.get(i).getLegCount();

      Animal a = animals.get(i);
      total += a.getLegCount();

      //total = the animal from the for loops legs
    }

    return total / animals.size();

    //For Each Loop
    float total = 0;

    for(Animal a : animals){
      total += a.getLegCount();
    }

    return total / animals.size();

*/





/*
MAZE:

block grid thing
decide what's filled in
*/
